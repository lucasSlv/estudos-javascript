var pessoa = {
 nome : 'Lucas Silva',
 idade : 17,
}

if(pessoa.idade >= 18){
    console.log(`Seu nome é ${pessoa.nome} e você tem ${pessoa.idade} anos e você é maior de idade.`)
} else {
    console.log(`Seu nome é ${pessoa.nome} e você tem ${pessoa.idade} anos e você não é maior de idade.`)
}